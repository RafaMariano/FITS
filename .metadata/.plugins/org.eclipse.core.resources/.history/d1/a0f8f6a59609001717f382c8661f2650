package br.inpe.log;

public enum FileSystemResult {
	CREATE_SUCCESSFUL,DELETE_SUCCESSFUL,MOVE_SUCCESSFUL;
	
	public static FileSystemResult convert(String str) {
        for (FileSystemResult demoType : FileSystemResult.values()) {
            if (demoType.toString().equals(str)) {
                return demoType;
            }
        }
        return null;
    }
}
